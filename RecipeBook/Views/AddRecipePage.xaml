<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:RecipeBook.Converters"
             x:Class="RecipeBook.Views.AddRecipePage"
             x:Name="AddRecipePageView">
    <ContentPage.Resources>
        <converters:Base64ToImageSourceConverter x:Key="Base64ToImageSourceConverter" />
        <converters:StringNotEmptyConverter x:Key="StringNotEmptyConverter" />
    </ContentPage.Resources>

    <ScrollView>
        <Grid RowDefinitions="Auto,*,Auto" Padding="20">
            <VerticalStackLayout Grid.Row="1" Spacing="20">
                <Label Text="Add New Recipe" FontSize="24" HorizontalOptions="Center" FontAttributes="Bold" Margin="0,0,0,20"/>

                <!-- Recipe Basic Info -->
                <VerticalStackLayout Spacing="10">
                    <Label Text="Recipe Title" FontAttributes="Bold" />
                    <Entry Placeholder="Enter recipe title" Text="{Binding Title}" />

                    <Label Text="Description" FontAttributes="Bold" Margin="0,10,0,0" />
                    <Editor Placeholder="Enter recipe description" Text="{Binding Description}" HeightRequest="100" />
                </VerticalStackLayout>

                <!-- Recipe Image -->
                <Frame Padding="15" Margin="0,10,0,0">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Recipe Image" FontSize="18" FontAttributes="Bold" />

                        <HorizontalStackLayout Spacing="10">
                            <Button Text="Pick Image" Command="{Binding PickImageCommand}" />
                            <Button Text="Take Photo" Command="{Binding TakePhotoCommand}" />
                        </HorizontalStackLayout>

                        <Image Source="{Binding SelectedImageBase64, Converter={StaticResource Base64ToImageSourceConverter}}" 
                               HeightRequest="200" Aspect="AspectFill" 
                               IsVisible="{Binding SelectedImageBase64, Converter={StaticResource StringNotEmptyConverter}}" />
                    </VerticalStackLayout>
                </Frame>

                <!-- Ingredients Section -->
                <Frame Padding="15" Margin="0,10,0,0">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Ingredients" FontSize="18" FontAttributes="Bold" />

                        <CollectionView ItemsSource="{Binding Ingredients}" EmptyView="No ingredients added yet">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="*, Auto" Padding="0,5">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="{Binding Name}" FontAttributes="Bold" />
                                            <Label Text="{Binding Quantity}" />
                                        </VerticalStackLayout>
                                        <Button Grid.Column="1" Text="Remove" 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveIngredientCommand}" 
                                                CommandParameter="{Binding .}" 
                                                BackgroundColor="Red" />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto, Auto" Margin="0,10,0,0">
                            <Label Grid.Row="0" Grid.Column="0" Text="Ingredient Name" />
                            <Entry Grid.Row="1" Grid.Column="0" Placeholder="Enter ingredient name" Text="{Binding NewIngredientName}" />

                            <Label Grid.Row="0" Grid.Column="1" Text="Quantity" />
                            <Entry Grid.Row="1" Grid.Column="1" Placeholder="Amount" Text="{Binding NewIngredientQuantity}" />

                            <Button Grid.Row="2" Grid.ColumnSpan="2" Text="Add Ingredient" Command="{Binding AddIngredientCommand}" Margin="0,10,0,0" />
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <!-- Steps Section -->
                <Frame Padding="15" Margin="0,10,0,0">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Preparation Steps" FontSize="18" FontAttributes="Bold" />

                        <CollectionView ItemsSource="{Binding Steps}" EmptyView="No steps added yet">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnDefinitions="*, Auto" Padding="0,10">
                                        <Label Grid.Column="0" Text="{Binding Text}" />

                                        <Button Grid.Column="1" Text="Remove" 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.RemoveStepCommand}" 
                                                CommandParameter="{Binding .}" 
                                                BackgroundColor="Red" />
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <VerticalStackLayout Spacing="10" Margin="0,10,0,0">
                            <Label Text="Step Description" />
                            <Editor Placeholder="Describe this step" Text="{Binding NewStepText}" HeightRequest="100" />

                            <Button Text="Add Step" Command="{Binding AddStepCommand}" Margin="0,10,0,0" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <Button Text="Save Recipe" Command="{Binding SaveRecipeCommand}" Margin="0,20,0,20" />

                <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}" />
            </VerticalStackLayout>

            <ActivityIndicator Grid.RowSpan="3" IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="Center" VerticalOptions="Center" />
        </Grid>
    </ScrollView>
</ContentPage>